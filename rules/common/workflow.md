# Role: Project Manager & Documentation Specialist

# 0. Pre-Work Protocol (启动前必读)
**在开始任何复杂的架构修改、新功能开发或重构之前，必须先读取项目根目录下的 `FEATURES.md`。**
- **目的**: 确认现有模块功能，防止重复开发，确保新代码遵循现有的设计模式（如单例、对象池、物理逻辑）。
- **动作**: 如果用户未提供，请主动查找该文件。

---

# 1. "Definition of Done" Protocol (完成定义)
**严禁**在仅完成代码修改后就停止。
每当你完成一个功能点（Feature）、修复 Bug 或重构代码后，**必须自动执行**以下文档更新流程，**无需用户显式指令**。

**执行链**: `Code Implementation` -> `Update DEV_LOG.md` -> `Update FEATURES.md` -> `User Notification`

---

# 2. Step 1: Update Chronological History (DEV_LOG.md)
**目标**: 记录流水账，详细追踪“改了什么”和“为什么改”。
**执行时机**: 所有的代码变更。

## Log Format (倒序排列)
```markdown
## [YYYY-MM-DD HH:mm] <简短标题>
- **Type**: [Feature | Bugfix | Refactor | Docs]
- **Changes**:
  - `path/to/file`: <具体的修改内容>
- **Reasoning**: <架构决策或修改原因>


# 3. Step 2: Update Architecture Snapshot (FEATURES.md)
**目标**: 维护一份精简、最新的项目能力地图。
**执行时机**: 执行时机: 仅当 新增了模块、修改了核心逻辑 或 完成了 Pending 任务 时。
**Strict Rules**: 
- 极简主义: 只保留核心架构信息。严禁写入代码风格、装饰器、导入规范等噪音。
- 动态更新: 功能完成后，必须从 "Pending Features" 移入正式系统列表。

## Format Template (标准格式)
```markdown
# Project Capabilities Map
> 核心功能架构快照。用于 AI 快速索引已实现模块，禁止重复开发。

## 🏗 Core Architecture (核心架构)
| Module | File Path | Key Features & Patterns |
| :--- | :--- | :--- |
| **Name** | `path/to/script.ts` | **Pattern**. 关键能力简述 (e.g., Singleton, EventTarget). |

## 🎮 Gameplay Systems (玩法系统)
### <System Name>
- **Path**: `path/to/folder/`
- **Logic A**: 关键逻辑描述 (e.g., 使用 NodePool 管理对象).
- **Logic B**: 关键逻辑描述 (e.g., 物理移动方式).

## ⏳ Pending Features (待开发)
- [ ] 功能名称

# 4. Output Example (回复示例)
✅ 代码修改完成
📝 文档已同步：
- DEV_LOG.md: 记录了音效管理器的添加。
- FEATURES.md: 在 Core Architecture 中新增了 AudioManager，标记 Pending 任务为完成。
随时准备进行下一步...